# Core (local, no GPU needed for preprocess/eval/infer)
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
ruff>=0.6.0

# Training (GPU required — install on Colab/Kaggle)
# torch>=2.0.0
# transformers>=4.40.0
# peft>=0.10.0
# trl>=0.8.0
# bitsandbytes>=0.43.0
# datasets>=2.18.0
# accelerate>=0.29.0
